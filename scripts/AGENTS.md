# Scripts (`scripts`)

## Role

Build-time helpers. `generate-sitemap.mjs` runs before `vite build` (`npm run build`).

## Prefer / avoid

- Keep scripts small and deterministic
- Do not put runtime app logic here

## See also

- [AGENTS.md](../AGENTS.md)
