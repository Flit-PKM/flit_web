/** Default title for a newly created note before the user edits it. */
export const DEFAULT_NEW_NOTE_TITLE = 'New note';

/** Default body for a newly created note before the user edits it. */
export const DEFAULT_NEW_NOTE_CONTENT = 'Note Content';

/** Relationship type used when appending one note after another. */
export const APPEND_RELATIONSHIP_TYPE = 'FOLLOWS_ON' as const;
